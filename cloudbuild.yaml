steps:
  # Step 1: Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'us-docker.pkg.dev/$PROJECT_ID/pogo-arcade/web:$COMMIT_SHA'
      - '-t'
      - 'us-docker.pkg.dev/$PROJECT_ID/pogo-arcade/web:latest'
      - '.'

  # Step 2: Push to Artifact Registry (us multi-region)
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - '--all-tags'
      - 'us-docker.pkg.dev/$PROJECT_ID/pogo-arcade/web'

  # Step 3: Deploy to Cloud Run in us-central1 (supports domain mapping)
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'pogo-arcade'
      - '--image'
      - 'us-docker.pkg.dev/$PROJECT_ID/pogo-arcade/web:$COMMIT_SHA'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '8080'
      - '--memory'
      - '256Mi'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '10'

images:
  - 'us-docker.pkg.dev/$PROJECT_ID/pogo-arcade/web:$COMMIT_SHA'
  - 'us-docker.pkg.dev/$PROJECT_ID/pogo-arcade/web:latest'

options:
  logging: CLOUD_LOGGING_ONLY
